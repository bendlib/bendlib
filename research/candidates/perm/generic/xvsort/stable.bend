import Base
import ./s1/p.bend as P1
import ./s2/p.bend as P2

def cross(-A: Data, xs: List<&2, Nat>, ys: List<&2, Nat>, h: P1.perm_by(~Nat, ~Nat.is_le, xs, ys)) -> P2.perm_by(~Nat, ~Nat.is_le, xs, ys):
  h
